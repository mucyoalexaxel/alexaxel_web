import { animated } from "@react-spring/web";
import { styled } from "@utils/stitches.config";

export const SocialIcon = styled(animated.div, {
  width: "45px",
  height: "45px",
  borderRadius: "50%",
  mx: 4,
});
